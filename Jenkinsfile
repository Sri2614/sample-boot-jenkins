pipeline {
        agent any

        stages {  
            stage ("first") {
                tools {
                   jdk "JDK1.8"
                }
                steps {
                    sh 'java -version'
                }
            }
           
       }
      
}
